# PRD：AI 采购助理（AI Procurement Assistant）

| 项 | 内容 |
|---|---|
| 版本 | v0.3（与实现同步：三模式可用 + 经验库兜底 + LLM 兜底） |
| 状态 | 主体已实现并通过测试（V1 落地）；剩余 F16/F17/F10 等见「实现状态」 |
| 日期 | 2026-09-10 起草 / 2026-09-14 更新 |
| 关联项目 | 个人作品集项目（求职 AI 产品岗 / 采购岗双线） |
| 变更记录 | v0.1 初稿；v0.2 新增 S6/S7/S8、F15-F18，流水线升级，吸收匹配 engine；**v0.3（2026-09-14）与实现对齐**：任务模式落地为「两表匹配/仅换算/完整比价」；匹配流水线改为 `钥匙精确→模糊→补充列→经验库兜底→LLM语义兜底→人工确认→回写`；经验库改为**兜底且可覆盖/删除/清空**；LLM 接入（opencode Go · 适配器/默认关/缓存/成本日志）；新增 parser_llm 非标解析 |

---

## 1. 背景与问题

采购员日常比价的真实流程：收到 3-10 家供应商报价单（Excel 格式各异）→ 手动复制粘贴对齐品类 → 含税/不含税换算 → 逐格找最低价 → 标红汇总 → 写比价结论。

**痛点量化**（来自一线实习观察）：
- 一份多供应商多轮报价的比价表，手工处理约 10-30 分钟，且易错（换算错税率、对齐错品类）
- 报价单格式不统一：合并单元格、供应商名在 sheet 标签、二次报价以新列追加、品类命名不一致
- 比价结论靠经验，无结构化沉淀

> 注：真实比价表格式锚点待用户提供实际文件后补充（用于 parser 规格与 hold-out 测试）。

**既有基础**：作者已在实习中自主开发过报价比对脚本（框选数据源 → 10 秒标红最低价）与对账报告生成器。本产品是其**产品化升级**：从一次性脚本 → 可复用、可扩展、面向非技术用户的工具。

## 2. 产品定位与目标

**一句话定位**：把采购员从「手动对齐、逐格换算、肉眼找最低价」中解放出来——上传报价单，10 秒完成对齐、换算、比价，并给出采购建议。

**V1 目标**：单机网页工具（Streamlit），覆盖「解析→对齐→换算→标红→建议」流水线，支持任务模式拆分使用。
**北极星指标**：一次完整比价任务的用户操作时间 ≤ 1 分钟（上传→出结果→导出）。

**V1 明确不做**（范围边界）：多用户/云端、ERP 对接、审批流、移动端。

## 3. 用户画像与核心场景

**画像**：企业采购员/采购实习生；Excel 重度用户；不会编程；对数据安全敏感（报价单是商业机密）。

**核心场景**：
| 场景 | 描述 | 频率 |
|---|---|---|
| S1 完整比价 | 收齐 3-10 家报价 → 全流程比价 → 导出比价表 | 每个采购项目 1-3 次 |
| S2 只换算 | 拿到含税价格表，只想同口径换算成不含税 | 高频、独立发生 |
| S3 只对齐 | 多家报价品类命名不一，只想对齐成一张矩阵表 | 中频、独立发生 |
| S4 二次报价更新 | 供应商回了新一轮报价，追加对比并看趋势 | 高频 |
| S5 采购建议 | 基于比价结果生成结构化建议（最低价/异常报价提示） | 随 S1 |
| S6 两表匹配补缺（VLOOKUP 类） | 合同↔组织映射、订单↔入库等补缺核对；两表共同列一般 100% 相似，模糊尾部+补充列进一步匹配 | **日常高频，使用最多** |
| S7 手动报价录入 | 供应商口头/微信报价，手动键入某家二次报价，与文件导入数据合并进同一矩阵 | 高频 |
| S8 模板整合输出 | 给出表数据，按上级指定模板整合成标准格式输出 | 高频 |

## 4. 用户故事

- 作为采购员，我希望**上传多个报价单 Excel**，系统自动识别每家的品类和价格，这样我不用手动复制粘贴。
- 作为采购员，我希望**一键在含税/不含税之间切换换算**（税率可指定/自动识别），保证比价同口径。
- 作为采购员，我希望**每行品类的最低价自动标红**，并注明是哪家供应商，这样我 3 秒看完结论。
- 作为采购员，我希望**单独用换算功能**——有时我根本不需要比价，只要同口径换算。
- 作为采购员，我希望**追加新一轮报价**时，历史轮次仍在，可以看到价格趋势（涨/跌）。
- 作为采购员，我希望**导出比价表**（保留标红和结论），能直接发给领导。
- 作为采购员，我希望系统**提示可疑数据**（如某家价格异常低于他人、品类疑似对不上），避免我被低价陷阱误导。
- 作为采购员，我希望**我的报价单数据不离开本机**（除我主动开启的 AI 功能外）。
- 作为采购员，我希望**手动键入某家的二次报价**（口头/微信报价），和文件导入的报价合并到同一张比价矩阵里。
- 作为采购员，我希望**按公司模板输出**——比价/匹配结果套进上级给的模板格式，交出去就是标准样子。
- 作为采购员，我**每次人工修正的匹配结果要被记住**——下次同样的"螺钉M4 vs 螺钉 M4×10"直接配对，不再重复处理，产品越用越快。

## 5. 功能需求

### 实现状态（2026-09-14，与代码/测试同步）

| 编号 | 功能 | 状态 | 实现位置 / 备注 |
|---|---|---|---|
| F01 | 任务模式选择 | 🟡 | 已落地 3 模式（两表匹配/仅换算/完整比价）；「仅对齐」未单列入口（对齐能力在完整比价内） |
| F15 | 两表匹配补缺 | ✅ | `core/matcher.py`；人工基准回测 精确率/召回率 **1.000** |
| F16 | 手动报价录入 | ❌ | 未实现 |
| F02 | 报价单解析（规则） | ✅ | `core/parser_rule.py`（表头探测+字段字典，xlsx/xls/csv） |
| F03 | 品类对齐 | ✅ | `core/aligner.py`（组合键+模糊+经验库兜底+置信度+人工确认） |
| F04 | 含税换算 | ✅ | `core/converter.py`（多税率、按列独立、公式行保护） |
| F05 | 最低价标红 | ✅ | `core/highlighter.py`（浅色彩虹色卡 + 自定义取色） |
| F06 | 结果导出 | ✅ | 下载 + 落 `data/outputs/`；**模板整合见 F17（未做）** |
| F07 | 样例数据 | ✅ | `tests/synth_quotes.py`（4 家 × 格式变体 + 脏数据 + ground truth） |
| F08 | LLM 兜底解析 | ✅ | `core/parser_llm.py`（规则优先、同接口、不可解析则安全退回） |
| F19 | LLM 语义兜底轮 | ✅ | 挂在 `matcher` 最后一级；带置信度/理由、来源标注、不自动入库 |
| F09 | 采购建议 | 🟡 | 规则版已做（`core/advisor_rule.py`：最低价/异常低价/缺报价）；LLM 版未做 |
| F10 | 二次报价趋势 | ❌ | 未实现（数据中已有"原始/议价报价"列可作输入） |
| F11 | 异常检测 | 🟡 | 异常低价预警已做；疑似品类错配预警未做 |
| F17 | 模板整合输出 | ❌ | 未实现 |
| F12 | 技能入口（意图路由） | ❌ | 地基已就位（`core/registry.py` 技能卡） |
| F18 | 匹配经验库 | ✅ | 已按**兜底**语义落地；可覆盖/删除/清空，以当前数据为准 |
| F13 | Word/PDF | ❌ | 远期 |
| F14 | 技能市场 | ❌ | 远期 |

## 5.1 LLM 的定位与边界（工程决策）

**定位：只做长尾兜底，不替代规则。** 规则引擎"准但脆"（格式/命名一变就要写规则），LLM"糙但能兜"（同物异名、非标格式）。因此：

| 用途 | 位置 | 触发条件 | 产物 |
|---|---|---|---|
| 非标解析（F08） | `parser_llm` | 规则解析质量不足（缺品名/价格） | 字段映射 → 规范表，标 `via=llm` |
| 语义配对（F19） | `matcher` 最后一级 | 精确/模糊/补充列/经验库都没结果 | 补缺值，标 `LLM语义` + 置信度 + 理由 |

**风控设计**：默认关；只吃尾部小批量；带置信度阈值（<0.6 视为未匹配）；结果可复核、**不自动入库**（人工确认才进经验库）；内容哈希缓存；单次运行调用上限；`logs/llm.log` 逐次留痕成本（含峰谷价）。**代价**：有成本/延迟（推理型需足额 max_tokens）、数据出境（报价单敏感时慎用）。

### P0（V1 必须）
| 编号 | 功能 | 说明 |
|---|---|---|
| F01 | 任务模式选择 | 下拉选择：两表匹配补缺 / 仅换算 / 仅对齐 / 完整比价。每个模式只执行所需模块，独立出结果、独立导出 |
| F15 | 两表匹配补缺（VLOOKUP 类，**核心**） | 吸收「采购数据通用匹配工具」引擎（core/matcher.py）：钥匙列精确→钥匙模糊→补充列辅助三轮瀑布 + 五种清洗 + 重复预警 + 原文件备份写回；已验证工程可用（带测试用例） |
| F16 | 手动报价录入 | 手动键入某家二次报价（品类/价格/税率/轮次），与文件导入数据合并进同一矩阵 |
| F02 | 报价单解析（规则引擎） | 识别表头（品类/型号/单位/含税价/不含税价/税率），支持多 sheet 多供应商；标准格式解析准确率 100% |
| F03 | 品类对齐 | 跨供应商品类矩阵对齐；同品异名模糊匹配（"螺钉M4"≈"螺丝钉"）+ 低置信度人工确认 |
| F04 | 含税换算 | 税率自动识别或手动指定；双向换算；全表同口径 |
| F05 | 最低价标红 | 矩阵内每行最低价标红 + 供应商标注 |
| F06 | 结果导出 | 导出 Excel，保留高亮与结论 sheet；支持按模板整合输出（见 F17） |
| F07 | 样例数据 | 内置脱敏样例（多格式变体），首次使用零门槛 |

### P1（V1 力争 / V2 初）
| 编号 | 功能 | 说明 |
|---|---|---|
| F08 | LLM 兜底解析 | 非标格式（合并单元格/表名即供应商）交给 LLM 提取；与规则引擎同接口可插拔；可配置开关（默认关） |
| F19 | LLM 语义兜底轮（第四轮） | 三轮瀑布后仍未匹配的行（小部分数据）批量交给 LLM 做语义级配对（"螺钉M4"vs"紧固件-螺钉"），带置信度与匹配理由；挂在现有瀑布上零重构 |
| F09 | 采购建议生成 | 规则版：最低价+异常报价检测；LLM 版：结合上下文给结构化建议（成本/风险/谈判点） |
| F10 | 二次报价趋势 | 追加轮次，展示价格变化方向与幅度 |
| F11 | 异常检测 | 报价显著低于中位数预警、疑似品类错配预警 |
| F17 | 模板整合输出 | 比价/匹配结果按上级模板整合成标准格式（复用对账报告生成器「模板+数据」引擎思路） |

### P2（V2+）
| 编号 | 功能 | 说明 |
|---|---|---|
| F12 | 技能入口（V2 核心） | 技能卡片列表 + 智能模式：自然语言输入（"把这两个表换算成不含税再比一下"）→ LLM 意图路由 → 调用注册的技能组合 |
| F18 | 匹配经验库（V2 核心·数据飞轮） | 人工确认/修正 + LLM 高置信结果沉淀为本地别名对照库；**兜底层**（精确→模糊→补充列→经验库→LLM→人工确认→回写）；**可覆盖/删除/清空，以当前数据为准**；全本地 JSON 零成本（骨架已实现：core/experience.py） |
| F13 | 多文件格式 | Word/PDF 报价单支持 |
| F14 | 技能市场式扩展 | 第三方技能接入（文件办公能手愿景） |

## 6. 技术架构

```
UI 层（Streamlit）：任务模式选择器 / 上传+手动录入 / 结果展示 / 导出（含模板整合）
        ↓
流水线编排（按任务模式裁剪）：
  两表匹配：钥匙精确②→ 钥匙模糊③→ 补充列④→ 经验库兜底 → LLM语义兜底⑤→ 人工确认⑥→ 回写经验库
  比价：parse → align（复用 matcher）→ convert → highlight → advise
        ↓
核心模块（core/，每个为独立函数 + 元数据注册卡）
  - matcher（两表匹配引擎，吸收自采购数据通用匹配工具，已验证）
  - experience（匹配经验库，骨架已实现）
  - parser_rule（规则解析）    - parser_llm（LLM 解析，同接口）
  - aligner（品类对齐）       - converter（税率换算）
  - highlighter（标红）       - advisor_rule / advisor_llm（建议）
  - templater（模板整合输出）
        ↓
注册表（core/registry.py）：{技能名, 描述, 输入输出 schema} —— V2 意图路由的地基（已实现）
```

**关键决策**：
1. **引擎吸收而非重写**：matcher 来自已上线使用的「采购数据通用匹配工具」exe（真实日频使用场景验证），原工具不动继续日常工作用，网页版吸收其引擎——开发速度与可信度双赢
2. **经验库兜底**（手写需求笔记的"经验库"洞察落地，2026-09-14 修订）：精确定位为**兜底**——精确/模糊/补充列都配不上时才用（含"对上多条"消歧）；经验库**可覆盖/删除/清空**，以当前数据为准（精确永远优先），不固化过时结论
3. **混合解析**：规则引擎优先（确定性 100%、零成本、离线），LLM 兜底（灵活、有成本）——同接口插拔，配置切换（base_url/key/model），兼容 OpenAI 格式（opencode GO / DeepSeek 均可直接接）
4. **模块即技能**：每个 core 函数带元数据卡注册，V1 的"任务模式"就是预置组合，V2 的"技能入口"只是把组合权交给 LLM 路由——架构无需重构
5. **本地优先**：除 LLM 功能外全部本地计算，报价单不出本机

## 7. 非功能需求

- **性能**：≤50 家供应商 × ≤500 行品类的比价 ≤ 10 秒（规则路径）
- **安全**：不上传任何服务器；LLM 功能默认关闭并明示"数据将发送至所配置的模型服务商"；样例数据全合成
- **可用性**：非技术用户 3 分钟内完成首次完整比价（内置样例）；错误提示说人话（如"第 3 张表没识别到价格列，点这里手动指定"）
- **兼容**：Windows + WPS/Office 生成的 xlsx

## 8. 数据指标

| 指标 | 定义 | 目标 |
|---|---|---|
| 北极星 | 完整比价任务端到端耗时 | ≤ 1 分钟（人工基线 10-30 分钟） |
| 解析准确率（分层） | 按格式难度分层：标准/半标/非标 | 标准 100% / 半标 ≥95% / 非标 ≥85%（不平均掩盖） |
| 换算正确率 | 税率换算结果与人工核对一致 | 100%（硬性） |
| 建议采纳率 | 用户导出时保留系统结论的比例 | ≥70% |
| 护栏指标 | 低置信度对齐的人工确认率 | 监控，>30% 说明对齐逻辑需改进 |

## 9. 迭代路线

| 版本 | 内容 | 状态/时间 |
|---|---|---|
| V1 | 三大模式（两表匹配/仅换算/完整比价）+ P1 的 LLM 兜底解析/语义兜底 + 经验库飞轮 | **✅ 主体已实现**（2026-09-14，9 套测试全过） |
| V1.5 | 真实脱敏 hold-out 验证（用户提供原始报价单）+ 文档/作品集打包 | 🔜 进行中（`data/raw_quotes/` 投放后即可跑） |
| V2 | 经验库全量飞轮（已提前落地并修订为"兜底"）+ 技能入口（技能卡 + LLM 意图路由） | 🟡 F18 已完成；F12 未做 |
| V3 | 模板库管理、多文件格式（Word/PDF）、技能市场式扩展 →「文件办公能手」 | 远期 |

## 10. 竞品与技术参考

- **现状竞品**：手工 Excel（市面主流）；比价类 SaaS（面向大企业采购系统，重流程而非轻工具）
- **差异化**：轻量单机、模块可拆、本地数据安全、AI 兜底可选
- **技术参考**（执行中补充 GitHub 调研结论）：Streamlit 表格组件（AgGrid）；SKILL.md 技能卡设计模式（opencode skills 生态）

## 11. 风险与对策

| 风险 | 对策 |
|---|---|
| 非标格式解析准确率不达标 | 分层指标透明呈现；LLM 兜底；真实格式 hold-out 持续迭代 |
| LLM 成本/隐私顾虑 | 默认规则路径离线可用；LLM 显式开关+配置化 |
| 合成数据失真导致"自测自嗨" | 掺脏数据+分层报告；邀请真实格式样本做 hold-out；样例生成逻辑公开在 tests/ |

## 12. 合成数据与测试策略

1. 5+ 种格式变体（标准表头/合并单元格/供应商名在 sheet 标签/二次报价追加列/品类乱序/税率不齐）
2. 故意掺脏：空值、同品异名、编码对不上、重复行
3. 每个样例带 ground truth，`tests/` 中自动跑分层准确率报告
4. hold-out：用户后续提供 1-2 份真实脱敏报价单作为终验集

# HANDOFF — AI 采购助理 · 项目交接文档

> 用途：当当前会话不可用（幻觉率升高/缓存失效/换新窗口）时，把本文件 + 项目打包交给新会话，即可无缝续做。
> 打包方式（用户说「打包」时执行）：
> ```powershell
> Compress-Archive -Path "C:\Users\zq130\Desktop\ai模型","C:\Users\zq130\Desktop\Skill实验\秋招投递台账.xlsx","C:\Users\zq130\Desktop\Skill实验\经历底库.md" -DestinationPath "C:\Users\zq130\Desktop\ai采购助理_交接包_$(Get-Date -Format yyyyMMdd_HHmm).zip"
> ```
> 新会话第一步：读本文件 + PRD.md，然后按「下一步」继续。

## 最新（2026-09-15·多表补全「自动配钥匙 + 表级延后 + 换组合复验」）

**用户口径（原话要点）**：钥匙列最多 4 把；用户可以自己点（例如确定的两列），也可以让系统自己找；
某张源表只有 1 把钥匙能对上时，第 1 轮先放后面，第 2 轮若能凑到 2 把就必须两把一起用，依旧只有 1 把就按 1 把匹配；
"有多的钥匙就抽查"；**操作要简单、结果不要一堆补丁**（复杂留在系统里）。

**做法（`core/table_filler.py`）**
- `pair_col()`：列名配对（同名/同义/近似）→ 不达标再试 **值域指纹** `value_domain_sim()`（两边唯一值 ≥5 且双向覆盖 ≥80% → 90 分「值域」）
- `SYNONYM_GROUPS` 增「分类族」(分类/类别/三级分类/品类)；`_polarity_conflict` 增「级别守卫」(一级分类 ≠ 三级分类)
- `plan_keys()`：每张源表**各自**定钥匙（你点的列 → 你手动映射过的列 → 按"加进来后歧义率最低"补位）；
  **只用模板里有值的列当正式钥匙**，只有当不足 2 把时才补一个"结构上能对上、现在还没值"的列留给级联
- `_combo_score()` 自检打分 = (歧义率, 覆盖率, 列数)；`_better()` 排序：歧义低 → 覆盖高 → 列数少
- **表级延后**：某源表本轮可用钥匙数 = 1 → 第 1 轮整表延后；第 2 轮起凑到 ≥2 把就用复合钥匙，仍 1 把就单把匹配
- **轮次语义修正**：折减改为按**跳数**（只用原值钥匙 → 不折减；用了补出来的值 → 每跳 ×0.9），
  轮数 = "真正补出东西的有效轮数"，所以"延后"不再吃掉一轮、也不再把直接填充压成 90 分
- **换组合复验** `audit_rounds/audit_seed`：另取组合再跑一遍（①少用一把 ②换池里另外两把 ③退到单钥匙），
  两次取值不一致的格 + 留空格 + 多候选格 → 第二张 Sheet「需人工确认」（**只提示，不改正式结果**）

**UI（`app.py`）**：钥匙列上限 5→4（不点也行=交给系统）；只多 1 个勾选框「自动配钥匙列」；
值域/延后/复验组数(默认2)/种子(默认42) 收进「高级」折叠；结果区 = **一行结论** + 明细折叠 + 需人工确认清单。

**回测（真实两表，报告 `data/outputs/多表补全回测报告.txt`）**
| 场景 | 填格 | 对 | 错 | 留空 | 歧义格 |
|---|---|---|---|---|---|
| 改前（只用「项目名称」） | 35 | 35 | 0 | 4 | **22** |
| 改后（系统自动配「项目名称+采购三级分类」） | 35 | 35 | 0 | 4 | **4** |
| 场景B：源表换成 `匹配表_001`（列名是 项目/类别） | 35 | 35 | 0 | 4 | **0** |

（剩余 4 个歧义格 = 源表自身有重复行且取值不同，算法无解，已进「需人工确认」清单。）

**验收样例**：`data/outputs/多表补全_真实回测_事业部.xlsx`（2 个 Sheet：模板补全结果 + 需人工确认 5 行）。
**测试**：`tests/experiment_table_fill.py` 30→**48 断言**；新增 `tests/experiment_table_fill_accuracy.py`（真实回测 9 断言）；
`tests/test_ui_smoke.py` 9→**17 断言**（含"点生成补全表跑完全链路"）。**14 套 ×2 遍全过**；build → **2026-09-15.01**。

## 项目定位（一句话）

把采购员从手动比价/两表匹配中解放出来：上传报价单 → 对齐/换算/标红/建议。**双重求职用途**：AI 产品岗作品集 + 采购岗数字化实证。作者：杨智潜（采购实习中，2027 届）。

## 文件地图

```
C:\Users\zq130\Desktop\ai模型\          ← 本项目根（所有文件只放这里）
├── PRD.md                              ← 产品文档 v0.2（评审后继续开发）
├── HANDOFF.md                          ← 本文件
├── app.py                              ← Streamlit 网页（两表匹配/仅换算 已可用）
├── ui_components.py                    ← 预览式点选组件（表头行/列点选、浏览文件）
├── 启动采购助理.bat                    ← 启动脚本（纯 ASCII+CRLF；端口占用则复用开页面）
├── core\
│   ├── matcher.py                      ← 两表匹配三轮瀑布引擎（吸收自已上线的 exe 工具）
│   ├── parser_rule.py                  ← 报价单解析（表头探测+字段字典→规范列）M2a
│   ├── aligner.py                      ← 跨供应商对齐（组合键+经验库+模糊+校验→比价矩阵）M2b
│   ├── advisor_rule.py                 ← 采购建议（规则版：最低价/异常低价/缺报价）M2c
│   ├── llm_client.py                   ← LLM 适配器（OpenAI 兼容/默认关/缓存/成本日志）M3
│   ├── experience.py                   ← 匹配经验库（人工确认沉淀→兜底命中，V2 数据飞轮）
│   ├── registry.py                     ← 技能注册表（V2 意图路由地基）
│   ├── converter.py                    ← 含税↔不含税换算（独立技能）
│   ├── highlighter.py                  ← 最低价标红（独立技能，网页+Excel 落色）
│   └── excel_io.py                     ← 读写/备份/写回/合并表头拆分/合并填充/公式行检测
├── tests\
│   ├── synth_quotes.py                 ← 合成报价单生成器（4 供应商×格式变体+脏数据+ground truth）
│   ├── smoke_pipeline.py               ← 端到端冒烟测试（51 断言，全过）
│   ├── experiment_gongju.py            ← 真实文件实验：汇总.xlsx 换算+写回（31 断言）
│   ├── experiment_pipei.py             ← 真实文件实验：001↔采购项目汇总 两表匹配（16 断言）
│   ├── experiment_match_accuracy.py    ← 客观回测：对人工基准算 P/R（报告进 data\outputs）
│   └── experiment_parser.py            ← 报价单解析测试：合成 4 家 + 真实件探针（21 断言）
│   └── experiment_align.py             ← 跨供应商对齐测试：同品异名/缺行多行/飞轮/校验（16 断言）
│   └── experiment_compare.py           ← 完整比价端到端：标红颜色回读+异常低价+缺报价（10 断言）
│   └── test_llm_client.py              ← LLM 适配层（离线）：默认关/缓存/上限/自检/不泄露（12 断言）
├── data\
│   ├── raw_quotes\                     ← 【用户投放】原始逐家报价单（parser 输入）
│   ├── ground_truth\                   ← 人工基准（只读）：标函分析表/事业部匹配结果/汇总表
│   ├── samples\                        ← 合成样例（与真实分开标注）
│   ├── outputs\                        ← 产出（比价结果、评估报告）
│   ├── README.md                       ← 各目录用途 + 评估纪律
│   └── experience.json                 ← 经验库（人工确认沉淀，随使用增长）
└── logs\app.log                        ← 崩溃/启动留痕（超 1MB 自动轮转 app.log.1）
```

## 关键决策记录（勿推翻）

1. **匹配流水线**（2026-09-14 修订）：`钥匙精确 → 钥匙模糊 → 补充列 → 经验库兜底 → LLM语义兜底(M3) → 人工确认 → 回写经验库`。**精确优先、经验库只兜底**（不抢答）；经验库可覆盖/删除/清空，不固化过时结论。
2. **引擎吸收而非重写**：matcher 来自 `C:\Users\zq130\.zcode\workspace\default\采购数据匹配工具\`（用户日常工作在用的 exe，原项目不动）。
3. **模块即技能**：每函数挂元数据卡（registry.py），V1 任务模式=预置组合，V2 技能入口=LLM 路由组合。参考 SKILL.md 模式（opencode skills 生态）。
4. **LLM 凭证**：`C:\Users\zq130\.local\share\opencode\auth.json`（opencode GO 套餐，OpenAI 兼容格式接入，勿打印 key）。备选：用户可充 DeepSeek。
5. **合成数据防自嗨**：掺脏数据+分层准确率报告+真实脱敏 hold-out（用户提供 1-2 份真实格式）。
6. **本地优先**：报价单不出本机，LLM 功能默认关、显式开关。

## 目录与文件纪律（每次更新必做）

- **本项目所有代码 / 文档 / 脚本 / 测试 / 产出，统一放 `C:\Users\zq130\Desktop\ai模型`**；不往桌面或其它目录散落
- 调试用临时脚本/中间文件只放 `%TEMP%\opencode`，用完即清（不落桌面）
- **每次更新后清理旧尾巴**（避免越攒越杂）：
  - 删 `__pycache__`（可自动再生，交付/归档前清）
  - 清 `%TEMP%` 下项目相关临时副本（`*_实验副本.xlsx`、`*_dbg*.xlsx`、`*_e2e.xlsx` 等）
  - 无引用的旧草稿/废弃文件及时删；**拿不准的先问用户**（如「demo话术.md」这类）
  - `logs/app.log` 超 1MB 自动轮转 `app.log.1`，无需手动
- **`启动采购助理.bat` 必须保持纯 ASCII + CRLF**：cmd 对批处理中文编码极敏感（UTF-8/GBK 混用→解析错乱，如 `start`→`tart`）。改它要用 PowerShell 精确写，**不要用会改编码/换行的编辑工具**
- 保持单一启动入口，不新增操作用小脚本

## 已完成（2026-09-10，含用户实测反馈迭代 v3）

- PRD v0.2（场景 S1-S8 / 功能 F01-F19 / 五级流水线 / V1→V3 路线）；误传截图相关内容已撤
- core 六模块 + 技能注册表 + 经验库，冒烟测试 **29/29** 通过（含列名税率识别/独立税率/插入位置/ai副本递增/写回断言）
- **换算交互 v4（用户反馈修订）**：**删除税率列配对机制**（曾把数据列数值误当税率导致计算错误）→ 每列独立换算；每列税率从**列名自动识别**（表头带「税率13%」「（1%）」等自动预填，parse_rate_from_name），识别不到用全局税率兜底，可手改；全局税率=识别不到时的默认
- **📂 浏览选择文件**：路径模式下点按钮弹 Windows 原生选择框（tkinter 子进程置顶），自动回填路径，替代手动粘贴
- Streamlit 页面：两表匹配补缺（全流程+写回原文件）、仅换算（全流程+写回原文件）、完整比价（占位）
- **预览式选择交互**（用户反馈迭代 v2）：表头行在原始预览中点行（带自动检测）、价格列/钥匙列/查补列全部在预览中点列头选（ui_components.py）
- **路径模式 + ai副本保险 + 写回原文件**（用户反馈迭代 v3）：
  - 文件来源二选一：粘贴路径/浏览选择（可写回）/ 上传（仅下载）
  - 写回前自动建备份 `ai副本+原sheet名`，已存在则 (1)(2) 递增不覆盖
  - 写回：换算新列插到源列右侧（合并单元格坐标尽力平移）；匹配结果用 exe 同款 write_result_in_sheet 就地写入
  - 查补列可选写入位置：新建列 / 填入匹配表已有列（就地更新，alias 机制）
  - xls/csv 不支持原位写回 → 提示改用下载
- 合成样例 4 供应商 + ground truth 生成器

## 已完成（2026-09-11，真实文件「汇总.xlsx」链路打通）

- **两行式（合并）表头拆分**（split_two_row_header）：上一行供应商名+本行子表头 → 「供应商 子表头」拆分命名，横向合并空位回填左邻，消除全部 Unnamed，重名自动 (1) 去重；UI 预览列头直接显示拆分名
- **税率单位规则 v3**：框内数字=百分比数值（1→1%、13→13%、"13%"/"0.13" 都认）；税率输入框下方实时预览解析结果（→ 解析为 13%），看不懂的列警告并走全局税率
- **写回改造 v2（writeback_convert）**：按列名全表定位已有新列（幂等重跑=覆写纠错，不重复插列，返回标注(覆写)）；新列复刻源列表头样式（供应商名竖向合并复刻）；合计行自动跳过（含「合计」不换算、写回原值不动）
- **公式行保护**（sheet_formula_rows）：openpyxl 读原始公式识别公式行；cols 参数限定只查所选价格列（比价表「最小值=MIN()」每行公式列不误伤）；换算跳过+写回保护 Row121/123 的 SUM 公式区
- **修复 openpyxl 3.1 insert_cols 不动合并单元格的坑**（_shift_merges_right 重写，插入前手术）：跨插入点的块合并（供应商名横跨两列 I1:J2）拆左右两半、右半随内容右移，新列表头不再被合并块吞掉；header_row=1 时 prev_r 越界保护；read_only 模式 EmptyCell 无 column 属性保护
- **真实文件实验 tests/experiment_gongju.py**：汇总.xlsx·2-工具清单 临时副本全流程 **31 断言全过**（拆分 8 + 税率识别 4 + 换算/跳过 7 + 备份/写回 10 + 幂等 2）；原文件零改动
- 冒烟测试扩至 **31 断言**全过（新增：合计跳过、两行表头拆分、写回幂等重跑）

## 已完成（2026-09-11 下午，两表匹配合并单元格支持 · 用户实测反馈）

- **顶行自动降级**（detect_header_block_bottom）：用户/自动检测把表头行点在两行合并表头的「顶行」（如 001.xlsx 选第 1 行）→ 自动落到第 2 行再拆分，页面有提示；判定依据=跨两行的竖向合并或顶行内横向合并块，且底行有实质内容
- **横向合并前缀回填**：「合同信息」横跨 D1:F1 → 子列名带完整前缀（合同信息 2026年11月），不再光秃秃
- **数据区合并填充**（unmerge_fill_data）：类别「消防维保」竖向合并 N 行 → 每行都带上该值（按 Excel 显示效果），钥匙列/补充列不再缺值；只填空格不动已有数据
- **两表匹配 UI 分组**：① 匹配表、② 钥匙表各自的「文件来源 → Sheet → 表头行预览」并排分组，两个 Sheet 下拉都显眼；单表读取出错不再吞掉另一表面板（try/except 隔离）；Sheet 列表读取失败自动改用字节流兜底；切 Sheet 后表头行选择 key 联动重置
- **新真实文件实验 tests/experiment_pipei.py（16 断言全过）**：001.xlsx·合同进度表 ↔ 采购项目汇总·Sheet1，验证顶行降级/前缀回填/合并填充/run_match（朗晴居二期等 ≥4 精确命中）/备份+写回临时副本
- 冒烟测试扩至 **39 断言**全过；experiment_gongju 改用「ai副本2-工具清单」快照作实验对象（真 Sheet 已被用户验收写回过，快照恒为原始 13 列布局），31 断言全过

## 已完成（2026-09-11 晚 v4，别名就地写回·预览与写回一致）

- **匹配预览口径修复（matcher.run_match）**：别名指向匹配表已有列时（如把钥匙表的「事业部」写入空着的「事业部」列），结果里**直接把值填进那一列**，不再单列成「事业部(钥匙表)」；写回同样就地更新。此前是「预览单列新列（重名自动加后缀）+ 写回追加表尾」，用户看不到值落进 A 列
- **写回指定列修复（excel_io.write_result_in_sheet）**：列名解析改用拆分名（认得第 1 行的竖向合并标签「事业部」），别名指向已有列时就地更新而非追加
- **卡顿**：读表+拆分+填充 st.cache_data 缓存（按文件字节为键）
- **崩溃日志 logs/app.log**：log_exception 接入表读取/匹配/下载/写回；侧边栏加 **build 版本标识**（当前 build 2026-09-11.4）
- ⚠️ **务必重启服务**：Streamlit 只热重载 app.py，core/ 模块改动需重启进程（关黑窗口重开 bat）；侧边栏 build 号变了才说明新代码生效
- 真实文件端到端验证（临时副本）：别名「事业部→事业部」→ 预览事业部列有值、写回就地更新、A 列填上、无多余列；冒烟 **45 断言**全过（新增别名就地/不重复追加断言），experiment_pipei 16、experiment_gongju 31 全过

## 已完成（2026-09-12，闪退排查 + 前端序列化隐患修复）

- **排查「网页闪退」**：无 logs/app.log（此前无捕获/旧进程），Windows 事件日志无 python 崩溃记录；用 Streamlit 官方 AppTest 跑「默认页→两表匹配→填真实文件」全流程 **0 异常**（两 Sheet 下拉、表头自动降级、合并填充均正常）→ 页面代码本身没问题
- **修掉两个真实前端隐患（白屏/报错的经典来源）**：
  1. `use_container_width` 已过弃用期（2025-12-31 后移除，本机 streamlit 1.63 每次渲染刷警告）→ 全部改 `width="stretch"`（app.py×2、ui_components.py×2）
  2. **pyarrow 序列化失败**：表头预览 DataFrame 列名混合类型（int 列名+"行号"）+ 单元格混合类型（int/datetime/str）→ `ArrowTypeError`、Streamlit 反复自动修列 → 预览统一转字符串、列名改「列1..列N」。修复后 AppTest 输出 pyarrow/Arrow/use_container 告警 **0 条**
- **崩溃留痕机制**：单文件 `logs/app.log`（每天几百字节）；**超过 ~1MB 自动轮转为 app.log.1**（只留一份，占用封顶，启动时检查）；每次会话写一行启动信息（build/python/streamlit 版本）；`sys.excepthook` + `threading.excepthook` 全局兜底；侧边栏「🐞 运行日志」显示当前大小，可直接查看/清空
- **启动脚本加固**（启动采购助理.bat，纯 ASCII+CRLF）：启动先探测 8501 → 本工具已在运行则**直接打开页面并退出**（不再重复启动报"端口占用"打不开）；被别的程序占用则明确提示；空闲才启动 Streamlit 并在 4 秒后用默认浏览器打开页面（`--server.headless true`，避开 Streamlit 自带跳转对夸克等浏览器不灵）
- build 号 → **2026-09-12.3**（侧边栏可见）；冒烟 45、experiment_gongju 31、experiment_pipei 16 全过
- **残留清理**：删除全部 `__pycache__`、`%TEMP%` 项目临时副本、`%TEMP%\opencode` 调试脚本（见「目录与文件纪律」）

## 闪退判读方法（下次）

- 侧边栏「🐞 运行日志」或 `logs/app.log`：
  - 有 `[时间] app started ...` 且**无** traceback → 不是代码崩溃，多半是**黑窗口被关/重复启动端口占用**导致服务停止
  - 有 traceback → 把日志发我，按 context 定位
- 确认跑的新代码：侧边栏 build 号（改 core/ 模块后必须重启 bat，Streamlit 不热重载 core/）

## 已完成（2026-09-13，启动自动化 + core 陈旧守卫）

- **根因**：用户报 `run_match() got an unexpected keyword argument 'experience'` —— 服务是昨天 15:57 起的旧进程，Streamlit 不热重载 `core/`，内存里是旧 matcher（app.py 传了 `experience` 但旧函数不认）。代码本身无误（已用 AppTest 验证）
- **启动脚本自动重启**：代码比服务新 → 自动杀旧服务并新代码重启；否则复用开页面。实测：旧 PID 21828 被自动结束、新服务 13:46:54 起、健康 ok、probe=STALE；再跑一次 probe=FRESH 且 PID 不变（复用）✓
- **应用内陈旧守卫**：`core/__init__.py::CORE_LOADED_MTIME` + `app.py::_core_stale()`；不一致则红色横幅 + 禁用匹配/换算/写回
- 验证：4 套测试连跑两遍全过；AppTest 0 异常/0 错误/0 告警

## 已完成（2026-09-12 晚 M1，经验库飞轮 + 客观回测）

- **目录分类**：`data\` 下分 `raw_quotes\`（用户投放原始报价单）/ `ground_truth\`（人工基准，只读）/ `samples\`（合成）/ `outputs\`（产出）；见 `data\README.md`。基准文件已从桌面拷入项目，两个旧实验改为**项目内路径**（不再依赖桌面）
- **经验库飞轮（第①级置顶命中）**：`run_match(experience=store)` —— 人工确认的配对第①级直接命中，不再进模糊/LLM 轮；支持**组合键**（多钥匙列）与**人工忽略**哨兵；新增统计「经验库命中/人工忽略」；结果状态新增「经验库/人工忽略」
- **人工确认回写 UI**（两表匹配页）：重复预警行从候选中确认一条、未匹配行标记忽略 → 写入本地经验库；侧栏经验库统计同步
- **客观回测** `tests/experiment_match_accuracy.py`：用人工基准 `事业部匹配结果` 逐行比对，**精确率 1.000 / 召回率 1.000 / 准确率 1.000**（36 行可评估，TP=33/FP=0/FN=0，1 行不在基准跳过）——组合键（项目+类别）消除了"重复预警"歧义，印证组合键更准；报告输出 `data\outputs\匹配准确率报告.txt`
- 冒烟 **51 断言**全过（新增经验库飞轮 6 项）；experiment_gongju 31、experiment_pipei 16、experiment_match_accuracy 全过；**4 套测试连跑两遍均通过**；AppTest 页面渲染 0 异常/0 告警
- build → **2026-09-12.4**

## 固定工作纪律（申明，往后每次照做）

1. 执行中发现更优方案 → **主动提出**并说明理由（动手前沟通）
2. 自动测试**至少跑两遍 + 人工复核输出**再汇报（减少低级错误）
3. **所有产出只放项目内、分类存放**；临时文件只放 `%TEMP%\opencode`；每步收尾清残留（`__pycache__`、临时副本、调试脚本）；桌面不留东西
4. `启动采购助理.bat` 保持纯 ASCII+CRLF（cmd 中文编码敏感）
5. **改 `core/*.py` 后无需手动重启**：双击 bat 会自动识别"代码比服务新"并重启；应用内也会显示"core 已更新，请重启"横幅并停用操作（见下）

## 启动/重启机制（2026-09-13）

- **启动脚本自动重启**（`启动采购助理.bat`）：探测 8501 → 本工具在跑时，比较"项目内 .py/.bat 最新修改时间"与"服务启动时间"：**代码更新了 → 自动结束旧服务并用新代码重启**；没更新 → 直接打开页面（复用）。端口被别的程序占用则提示。
- **应用内陈旧守卫**（`app.py` + `core/__init__.py`）：`core.CORE_LOADED_MTIME` = core 包被导入时的最新 mtime；每次运行与磁盘当前 mtime 比对，不一致 → 顶部红色横幅「core 已更新，请重启」并**禁用匹配/换算/写回**按钮（避免再出现"参数不认识"这类误导报错）。
- 教训来源：Streamlit 只热重载 app.py，**不重载 core/** —— 改了 core 不重启就会跑旧逻辑（曾报 `run_match() got an unexpected keyword argument 'experience'`）。

## 已完成（2026-09-13 M2a，报价单解析器）

- **`core/parser_rule.py`（新，技能卡已挂）**：表头行自动探测（关键词打分）+ 字段关键词字典映射 → 规范列（序号/品名/规格/单位/数量/含税单价/不含税单价/税率/品牌）
  - 真实件关键坑：真实报价单常见 **「单价（元）\n税率1%」同格**、表头在第 2 行 → 已修「价格优先于税率」+ 表头内嵌税率提取（`单价（元）\n税率1%`→ 价格列 + 税率 1%）
  - 合并列「品名及规格/品名及型号」自动拆 品名+规格；文本价 `¥1,234.56` 可解析；跳过合计/全空行
  - 供应商名默认取**文件名**（"物资采购清单（利源通）(1).xls"→利源通），sheet=分类；支持 xlsx/xls/csv
- **`tests/experiment_parser.py`（21 断言全过）**：合成 4 家逐项（表头探测/字段映射/文本价/合并列拆分/同品异名/缺行多行）+ 真实件探针（表头行=2、供应商=利源通、字段齐全、行数 74）；报告 `data/outputs/解析报告.txt`（如实标注"真实样本仅 1 份，不作准确率宣称"）
- 冒烟扩至 **56 断言**；5 套测试**连跑两遍全过**；build → **2026-09-13.2**

## 已完成（2026-09-13 M2b，跨供应商对齐）

- **`core/aligner.py`（新，技能卡已挂）**：多份报价 → 比价矩阵（行=品类，列=供应商 + 最低价/最低价供应商）
  - 组合键默认「品名+规格」（`key_fields` 可传，页面自选）；`norm_text` 规范化（全/半角、空格、×/x/*、括号、大小写）
  - 匹配四级：**① 经验库置顶 → ② 规范化精确 → ③ 模糊 → ④ 新品类**；模糊用**多信号**（ratio + partial + **字符袋**，应对中文乱序/子串异名）；规格作一致性下限
  - **置信度分级**（高/中/低）+ **校验变量**（单位/数量不一致降级）；仅字符袋命中（乱序）保守降为「中」，进 `low_confidence` 待人工确认
  - 口径：`price_field` 取价（默认**不含税单价**），缺该口径用税率现算（缺税率用全局兜底）
  - `record_alias()`：把「供商品名组合键 → 标准品组合键」写经验库（`raw=True`，避免组合键分隔符被清洗）
  - **修**：`experience.norm_vals` 增加 `raw=True`（此前对组合键又做 `clean_value`，会把 `\x1f` 当不可见字符删掉导致键对不上）
- **`tests/experiment_align.py`（16 断言全过）**：4 家对齐行数=7、同品异名不拆行、D 缺行/多行、最低价供应商=GT、飞轮确认后走「经验库」且置信度高、单位不一致降级
- 冒烟扩至 **59 断言**；6 套测试**连跑两遍全过**；build → **2026-09-13.3**

## 已完成（2026-09-13 M2c，完整比价页面 + 标红色卡 + 规则建议）

- **完整比价页（app.py，替换占位）**：① 选报价单（项目 `data/raw_quotes` 目录多选 / 上传多件 / 粘贴路径）→ 解析预览（供应商名可改、显示表头行/识别字段/警告）→ ② 对齐配置（**对齐键字段多选**、口径**默认不含税**、模糊阈值）→ ③ 比价矩阵 + 低/中置信度**人工确认**（一键写经验库）→ ④ **标红配色**（浅色彩虹 8 档预设 + 自定义取色器，**默认浅黄**）→ 导出 `data/outputs/` → ⑤ 规则版采购建议
- **`core/highlighter.py`**：配色参数化 `min_fill/min_font`（`#RRGGBB`→ARGB）、`PRESETS` 浅色彩虹 8 档、默认浅黄；导出回读校验通过
- **`core/advisor_rule.py`（新，技能卡）**：最低价供应商、**异常低价预警**（最低价 < 行中位数×0.5）、缺报价提示、汇总
- **`core/aligner.py`**：`low_confidence` 增加 `alias_key/canon_key`，供页面一键回写经验库
- **修**：app.py 之前**没导入 pandas**（完整比价页用 `pd.DataFrame`）→ AppTest 端到端暴露并修复
- **`tests/experiment_compare.py`（9 断言）**：全流程 + 标红颜色回读（自定义浅绿 FFC6EFCE/FF006100）+ 异常低价/正常行不误报 + 缺报价；报告 `data/outputs/比价报告.txt`
- 冒烟扩至 **62 断言**；7 套测试**连跑两遍全过**；AppTest「解析→对齐」UI 全流程 0 异常/0 错误/0 告警；build → **2026-09-13.4**

## 已完成（2026-09-14，经验库改为「兜底」+ 可覆盖/可删/可清空 + 备注列）

- **流水线顺序修订**：`钥匙精确 → 钥匙模糊 → 补充列辅助 → **经验库兜底** → 空白`
  （原为经验库第①级抢答）——**精确永远优先**，经验库只在规则都配不上时上场（含"对上多条"消歧）；所有方法都用不上 → 留空
- **解决"记录压过精确"的坑**：用户场景「新之助→小爱」被记录后，第二次精确配到「野原」——现在**填野原**（精确赢），不再被旧记录覆盖
- **经验库管理（解决"改不动"）**：`record` 改**覆盖语义**（同一左键后确认者赢）；新增 `list_pairs/remove/clear`；侧栏「🧠 经验库」面板可**查看/删除单条/清空全部**
- **结果「备注」列**（匹配结果 + 比价矩阵「对齐备注」）：标注来源与相似度（模糊/经验库兜底/重复预警/忽略），来源列（匹配轮次）保持不变
- **模糊项默认不入库**：确认面板新增「模糊命中的行 → ☑ 仍要记住」（勾了才入库，供下次兜底/消歧）
- **已知限制**：**同一键值的重复行**（多行键相同、仅取值不同）经验库无法消歧（只存键组合）→ 仍会重复预警需人工选；"键相似但不同"的多候选可消歧
- **aligner 同步**：顺序改 `规范化精确 → 模糊 → 经验库兜底 → 新品类`；矩阵加「对齐备注」
- 冒烟 **75 断言**（新增覆盖/精确优先/兜底/消歧/备注断言）；7 套测试**连跑两遍全过**；回测仍 1.000；AppTest 两模式+比价流程 0 异常/0 错误；build → **2026-09-14.1**

## 已完成（2026-09-14 晚 M3，LLM 适配层就绪 · 凭证待通）

- **`core/llm_client.py`（新）**：厂商无关的 OpenAI 兼容适配器
  - provider 预设（opencode-go/opencode/deepseek/openai/ollama）+ 可自定义 base_url/model
  - 凭证：显式/`LLM_API_KEY` → 否则读 `~/.local/share/opencode/auth.json`（**不打印 key**）
  - **默认关**（enabled=False 不发任何请求）；**内容哈希缓存**（`data/llm_cache.json`）；**成本留痕** `logs/llm.log`；**单次运行调用上限**；仅标准库（urllib）
  - `self_test()` 连通性判读（正常 / 余额不足 / 认证失败）
- **侧栏「🤖 LLM」面板**：服务商/模型/开关、保存配置（`data/llm_config.json`）、**一键连通性自检**、清缓存
- **`tests/test_llm_client.py`（12 断言，离线注入假 HTTP）**：默认关不发请求、缓存命中、调用上限、无凭证、自检三种判读、留痕、**不泄露密钥**
- ⚠️ **凭证实测结论（2026-09-14）**：
  - `opencode-go` key 直连 Zen → **余额不足**（`CreditsError: Insufficient balance`，opencode.ai/auth 充值）
  - `opencode` key 直连 Zen → **无效 key**
  - `deepseek` key → **无效/失效**
  - → **M3 的语义兜底/非标解析暂被凭证阻塞**；适配层与界面已就绪，**拿到有效 key 即可用**（侧栏自检确认）
- 冒烟 75 + 8 套测试**连跑两遍全过**；AppTest 三模式 0 异常/0 错误；build → **2026-09-14.2**

## 已完成（2026-09-14 晚 M3b，LLM 语义兜底打通 · 凭证已通）

- **凭证打通**：用户提供 opencode **Go** 套餐 key（写入 `~/.local/share/opencode/auth.json` 的 `opencode-go`，**不入项目、不打印**）
  - **Go 端点**：`https://opencode.ai/zen/go/v1`（需 `x-opencode-session` 头；Zen 端点 `…/zen/v1` 不支持该套餐）
  - **模型**：`deepseek-v4.1-flash`（**推理型**：max_tokens 要给足 ~4000，否则预算被 reasoning 耗尽→正文为空；实测 2000 会空）
  - **定价/消耗**：off-peak $0.15/$0.60 每 1M（peak 翻倍：工作日 01-04、06-10 UTC）；`llm_client` 内置 `estimate_cost` + `logs/llm.log` 逐次留痕（实证：一次兜底 ≈ **$0.0002**）
- **`core/matcher.py` LLM 语义兜底**：顺序 `精确→模糊→补充列→经验库兜底→**LLM语义兜底**→空白`；只吃尾部小批量，**一次批量调用**（≤100 候选），返回 JSON（row/key/confidence/reason），置信度<阈值(0.6)视为未匹配；来源标 `LLM语义`，备注含置信度+理由；**不自动入库**（人工确认才回写）
- **app.py**：`llm=LLMClient()`（读 `data/llm_config.json`）传入 run_match；指标新增「LLM兜底」；「模糊命中的行」区含 LLM 行可「☑ 仍要记住」；侧栏「🤖 LLM」面板可切 provider/模型/开关/自检/清缓存
- **`data/llm_config.json`**：provider=opencode-go · model=deepseek-v4.1-flash · enabled=true
- 冒烟 **81 断言**（新增 LLM 兜底 6 项，离线假客户端）；8 套测试**连跑两遍全过**；AppTest 三模式 0 异常/0 错误；build → **2026-09-14.3**

## 已完成（2026-09-14 晚 M3c，非标解析 parser_llm）

- **`core/parser_llm.py`（新）**：规则优先、非标才用 LLM，**与 parser_rule 同结构**（多一个 `via` 标记 rule/llm）
  - `rule_quality()` 判定（有品名+有价格+有数据行）→ 够好直接返回，**零 LLM 调用**
  - 规则不行：把该 Sheet 前 15 行原样给模型 → 返回 `{header_row, mapping{字段:1-based列}, supplier}` → 用 `build_canonical` 组装规范表
  - `parse_quote_auto(path/data, llm, force_llm)`：统一入口；LLM 未启用/返回乱码 → **安全退回规则**并附警告
  - 只发必要内容 + `llm_client` 内容缓存 → 成本可控
- **`core/parser_rule.py`**：抽出公共 `build_canonical(headers, data_rows, mapping, ...)`（rule/llm 共用，避免重复）；原测试 21 断言仍全过
- **app.py 完整比价页**：勾选「🤖 非标文件用 LLM 解析」（默认跟随开关）；解析预览新增「解析方式」列（规则/LLM）；有 LLM 解析时给提示
- **真实 LLM 验证**：非标件（描述/采购价/增值税）→ LLM 正确映射 品名←描述、单位←计量单位、数量←需求数量、含税单价←采购价、税率←增值税，值/税率解析正确 ✓
- **`tests/experiment_parser_llm.py`（9 断言，离线）**：非标触发 LLM、映射生效、标准件零调用、未启用退回、乱码安全退回
- 冒烟 **83 断言**；**9 套测试连跑两遍全过**；AppTest 三模式 0 异常/0 错误；build → **2026-09-14.4**

## 已完成（2026-09-14 收尾 A：文档 + GitHub 作品集）

- **README.md 重写**（作品集级）：定位/痛点/三模式/架构/快速开始/目录/**9 套测试与证据表**/隐私/路线图
- **PRD v0.3**：新增「实现状态」表（F01–F19 逐项 ✅/🟡/❌）与「LLM 的定位与边界」；迭代路线同步
- **仓库发布**：`https://github.com/YangMikey/ai-procurement-assistant`（**public**，默认分支 main）
  - 署名：`Zhiqian Yang <YangMikey@users.noreply.github.com>`；本地（仅本仓库）已设 `http.version=HTTP/1.1`、`http.sslBackend=schannel`
  - `.gitignore` 排除真实数据/日志/产出/缓存：远端核验 `data/` 仅含 `README.md / llm_config.json / samples`（无 ground_truth / raw_quotes）
  - 推送环境注记：github.com:443 国内偶发连不上（api.github.com 正常）→ 重试 + 上述 git 配置可稳过
- 待办：**真机 hold-out**（把原始逐家报价单放 `data/raw_quotes/` → 跑真实解析准确率，作品集最有说服力的证据）

## 已完成（2026-09-14 路线B，功能补齐）

- **F01 仅对齐**：新增第 4 个任务模式（复用比价的对齐段，产出并导出对齐矩阵，不做标红/建议）
- **F16 手动报价录入**：完整比价页「✍️ 手动录入报价」（供应商名 + 可编辑表 + 价格口径）→ `parser_rule.manual_quote()` 产出与解析器同结构，直接并入对齐
- **F17 模板整合输出**：`core/templater.py::fill_template()` —— 上传模板 xlsx、指定 Sheet/起始单元格/标题，整块写入（保留模板格式），落 `data/outputs/`
- **F09 LLM 采购建议**：`core/advisor_llm.py::advise_llm()` —— 规则版结论作上下文，LLM 出①成本②风险③谈判；复用 llm_client（缓存/成本/上限）。实测一次约 **$0.0039**
- **F11 疑似品类错配**：`advisor_rule.advise(low_confidence=...)` 接入对齐低置信度 → 「疑似错配」预警 + 文案
- **公共重构**：`parser_rule.build_canonical()` 抽出（rule/llm/manual 共用）
- **新测试** `tests/experiment_features_b.py`（13 断言，离线）；冒烟扩至 **86 断言**；**10 套测试连跑两遍全过**；AppTest 4 模式 + 仅对齐 UI 流程 0 异常/0 错误；build → **2026-09-14.5**
- **PRD/README 同步**：状态表 F01/F09/F11/F16/F17 → ✅；README 增「手动录入/仅对齐/模板输出/LLM 建议」与测试表
- **仍缺**：F10 二次报价趋势（需多轮价格数据模型，另行评估）

## 已完成（2026-09-14 对齐「尽量填充」+ 备注规则）

- **对齐判定改为"尽量填充"**（用户口径：非 100% 匹配的很少，人工有精力复查）：
  - 阈值默认 **60**（页面滑条 30~100 可调；越低越"尽量填"）
  - **去格式后完全相同 → 精确命中（不备注）**；**其余命中一律写进「对齐备注」：方式 + 相似度**（`模糊 75%`、`经验库兜底`）
  - **规格不一致仍会填充**（不再直接否掉）→ 但**降为低置信** + 备注 `规格不一致(xx%)` + 进人工确认
- 实测：`螺钉M4 ↔ 螺丝钉M4×10` 相似 75% → 阈值 60 下合并并备注；`紧固件-螺钉 ↔ 螺钉M4×10` 仅 ~45% → 仍不合并（需经验库/后续 LLM 判同）
- 测试：`experiment_features_b` 增至 **19 断言**；10 套测试**连跑两遍全过**；AppTest 4 模式 0 异常；build → **2026-09-14.6**

## 已完成（2026-09-14 新技能：多表补全 F20）

- **需求**：模板（第1列=钥匙，其余=要补列）+ 多张源表 → 自动把各源的信息补进模板；非 100% 匹配按置信度上色，找不到留空+底色；表下只写图例/统计备注
- **`core/table_filler.py`（新）**：
  - **列供给自动发现** `discover_supply`：列名匹配（**同名100 > 同义95（内置同义词族：结束/截止、金额/价格、编号/编码…）> 近似(多信号) ≥阈值70**）；**含税/不含税等"极性冲突"直接排除**；一列多源按匹配度 + 有值数排序
  - **逐行取最优**：每行遍历候选源，取"钥匙匹配度最高"的值（如 B 公司在源2只有 ~60%、源3 精确 → 取源3）
  - **钥匙打分** `_key_sim`：完全相同=100；否则 ratio / partial-10 / bag-5 取最大 → **不满分**（避免 `A公司集团` vs `A公司` 被静默当 100%）
  - **置信分档上色**：无色=100%／浅黄=80–99%／浅灰=40–80%／浅蓝=20–40%／浅红底=未匹配(<20 或该列无人供)
  - `export_filled`：写 xlsx + 颜色 + **表格下方图例/统计备注**（不列来源表）
- **UI**：第 5 个任务模式「🧩 多表补全」（模板上传→选钥匙列→源码多选上传/raw_quotes→各选钥匙列→列供给可改→阈值/最低分→生成→下载）；默认只出新文件
- **测试** `tests/experiment_table_fill.py` **19 断言**；冒烟 **89 断言**；**11 套测试连跑两遍全过**；AppTest 5 模式 0 异常；build → **2026-09-14.7**

## 已完成（2026-09-14 表格美化 F21 · 独立 + 业务共用）

- **`core/theme.py`（新）**：统一美化主题
  - 语义色板（浅底深字）：结论/推荐=浅绿 `E2EFDA/375623`；风险=浅红 `FCE4E4/9C0006`；缺失=浅灰 `F2F2F2`；置信 高/中/低=`FFF2CC/DDEBF7/E4DFEC`
  - `style_sheet`：表头深底白字+加粗+换行、**列宽自适应**（中文按 2 字符，8~40，>5000 行跳过）、冻结数据首行、AutoFilter、**数字格式**（金额两位小数、整数千分位、比例百分比、日期）、可选**斑马纹**/**每行最低值高亮**（仅"可比数值列"，自动跳过序号/日期/时间/数量等）、表下页脚（生成时间/来源/图例）
  - `beautify_bytes`：独立美化（默认**新增 sheet `美化-<原名>`**，原表不动；`in_place=True` 就地美化，调用方先备份）
- **第 6 任务模式「🎨 表格美化」**：选文件→Sheet→表头行→选项（斑马纹/最低值高亮/写回原Sheet）→ 生成/下载；写回前自动 `ai副本<sheet>`
- **业务导出统一走主题**：`excel_io.export_df_bytes`/`_export_single_sheet`、`highlighter.export_highlighted`（**最低价默认改"结论绿"**，PRESETS 首位=结论绿）、`table_filler.export_filled`（分档色改主题色：中=浅蓝、低=浅紫、缺失=浅灰）
- **实验样例（用户文件）**：`data/ground_truth/供应商信息反馈.xlsx`（表头第2行）→ 生成 `data/outputs/美化_供应商信息反馈.xlsx`
- 测试：新增 `tests/experiment_beautify.py` **18 断言**（含真实文件：表头/冻结/工资列最低值高亮/不可比列不误标/原表数据未动）；全套 **12 套测试连跑两遍全过**；AppTest 6 模式 0 异常；build → **2026-09-14.8**

## 已完成（2026-09-14 美化修复 v2：备份不被覆盖 + 数据默认居中）

- **修 bug（用户实测发现）**：美化"写回原 Sheet"时，先用内存里的**旧字节**（不含刚建的备份）覆盖文件 → **备份被冲掉**（提示有 `ai副本Sheet1`，文件里却没有）
  - 新增 `core/theme.py::beautify_file_in_place(path, sheet, header_row, options)`：**先建 ai副本 备份 → 重读含备份的最新字节 → 就地美化 → 写回**；app 写回分支改调它
  - 回归测试：备份 sheet 与美化后 sheet **同时存在**、备份内容=美化前、**二次执行备份递增且旧备份仍在**
- **数据默认居中**：`style_sheet` 数据区改为 `horizontal=center`（表头也居中）；不再"数字右对齐"
- 新增 `tests/experiment_beautify.py` 断言至 **23**（含备份回归 4 条）；12 套测试**两遍全过**；AppTest 6 模式 0 异常；build → **2026-09-14.9**
- 样例已重生成：`data/outputs/美化_供应商信息反馈.xlsx`（居中 + 工资列最低值高亮 + 页脚）

## 已完成（2026-09-14 多表补全 v2：多钥匙 + 分层匹配 + 自动级联）

- **多钥匙列（≤5）**：模板侧多选（点击顺序=优先级）；**源表不用选钥匙**——系统按模板钥匙列自动在每张源表里找对应列（同名/同义/近似 ≥ 列名阈值，默认 70），表里有几列对上就用几列
- **分层匹配**：钥匙层 **n→1**（逐层减少钥匙列），层内**先精确（索引 O(1)）后模糊**；命中即停 → 提高召回又不牺牲精确
- **自动级联（最多 4 轮 + 提前收敛）**：某轮新补出的值 **≥80% 才"升级为钥匙"** 参与后续轮；每轮开始快照可用钥匙（本轮新补下轮生效）→ **多跳置信 ×0.9/跳**，备注写 `@轮次·N钥匙·歧义` 
- **歧义处理**：同钥匙命中多行 → 取值相同则直接用；不同则取第 1 条 + 降为低置信 + 备注 `歧义N行`
- **UI**：模板钥匙列多选；源表处显示"🔑 自动识别到钥匙列：…"；新增参数 最大级联轮数(默认4)、升级门槛(默认80)；结果区显示 级联轮数/间接补全格/歧义格
- 测试 `experiment_table_fill.py` 扩到 **29 断言**（多钥匙区分、分层降级命中、两跳级联、低置信不升级、三跳链轮数=3、歧义取值、**真实文件双钥匙命中率≥90%**）；**12 套测试连跑两遍全过**；AppTest 6 模式 0 异常；build → **2026-09-14.11**

## 已完成（2026-09-14 修复：多表补全「列供给」下拉 KeyError）

- **Bug（用户实测）**：`app.py` 拼列供给下拉标签时用了候选字典的 `_c['name']`，而 `discover_supply` 的候选键为 `source/col/score/how/filled`（**无 name**）→ `KeyError: 'name'`（从多表补全第一版就在，只是此前没产生候选所以没触发）
- **修法**：新增 `core/table_filler.py::supply_option_label(cand, sources)`（标签 = `源表名【源列】(方式,分数)`），app 改调它
- **防复发**：新增 **UI 冒烟测试** `tests/test_ui_smoke.py`（AppTest：6 模式渲染 + 多表补全「模板路径 + raw_quotes 源表」走到列供给，9 断言）——这类"界面引用了不存在的键"以后会被它抓住；`experiment_table_fill` 也加标签断言（30 断言）
- 12 套测试两遍全过 + UI 冒烟两遍；build → **2026-09-14.12**

## 下一步（按优先级）

0. **用户验收**：重启 bat（build 2026-09-14.12）→「多表补全」：模板选 1~5 钥匙列 → 源表直接上传 → 看「列供给」下拉（现在显示 `源表名【列名】(方式,分)`）与级联轮数
1. **真机 hold-out**：原始报价单放 `data/raw_quotes/` → 真实解析准确率
2. **可选**：业务导出"结论+明细"双 sheet；LLM 判同物；F10 二次报价趋势；bat 的 B 项提速

## 求职主线关联（勿丢）

- 投递台账：`C:\Users\zq130\Desktop\Skill实验\秋招投递台账.xlsx`（5 sheet：投递台账/渠道/状态/意向/补强计划）
- 经历底库：`C:\Users\zq130\Desktop\Skill实验\经历底库.md`（E1-E6，含本工具与本项目素材）
- 岗位意向：主攻采购采销（制造业/新能源/低空，排除地产物业+外贸）；AI 产品岗 10 月投（本项目=核心作品）
- 面试故事线：真实工具（exe，在用）→ 产品化（本项目）→ 经验库飞轮 → V2 技能入口 → 文件办公能手愿景
